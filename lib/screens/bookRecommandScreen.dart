import 'package:flutter/material.dart';

class BookRecommandScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('book recommand!'),
    );
  }
}
